import { CreateBoardValidationResult } from './create-board-validation-result';

describe('CreateBoardValidationResult', () => {
  it('should create an instance', () => {
    expect(new CreateBoardValidationResult()).toBeTruthy();
  });
});
