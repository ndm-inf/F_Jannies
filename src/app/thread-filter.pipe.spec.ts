import { ThreadFilterPipe } from './thread-filter.pipe';

describe('ThreadFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreadFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
